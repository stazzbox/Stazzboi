# Stazzboi.com Homepage Videos

Place homepage video files in this folder.

Supported formats: `.mp4`, `.webm`, `.mov` (MP4 is recommended for browser compatibility).

After adding a video, add its filename to `videos.json` under `videos`. The homepage randomly selects one entry when the page opens and attempts autoplay with muted audio, which is required by many browsers for reliable autoplay.
